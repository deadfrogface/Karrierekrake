# Development / CI / packaging tools (not needed by end users).

-r requirements-runtime.txt
pytest>=8.0
# CI splits the Windows unit-tests job into equal-time shards (not a runtime dep).
pytest-split>=0.11.0,<1
pyinstaller>=6.3
ruff>=0.6
radon>=6.0
vulture>=2.11
# Cover-letter Writer prompt optimization only (not shipped in runtime).
dspy==3.3.1
# Lifecycle E2E factory (PR33) — optional property/time helpers; not required at runtime.
hypothesis>=6.100.0
freezegun>=1.5.0
# Shared mobile contracts (PR37) — schema validation in CI.
jsonschema>=4.22
# Supply-chain / SAST (PR41) — orthogonal to gitleaks/OSV binaries in CI.
pip-audit>=2.7.0
bandit>=1.7.9
# Windows unit test of the manual benchmark kills a real Job Object tree
# (tests/test_perf_benchmark_rss.py). Not a desktop runtime dependency.
psutil>=5.9
# Streamlit UI removed (ui/app.py deleted) — do not re-add for desktop builds
