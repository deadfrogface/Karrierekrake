# Development / CI / packaging tools (not needed by end users).

-r requirements-runtime.txt
pytest>=8.0
pyinstaller>=6.3
ruff>=0.6
radon>=6.0
vulture>=2.11
# Cover-letter Writer prompt optimization only (not shipped in runtime).
dspy==3.3.1
# Streamlit UI removed (ui/app.py deleted) — do not re-add for desktop builds
