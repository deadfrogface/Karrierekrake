[pytest]
markers =
    network: optional live network probes (not required for CI green)
    e2e: full-product end-to-end / chaos megapass (offline, fake providers)
filterwarnings =
    ignore::DeprecationWarning
