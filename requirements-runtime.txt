# Runtime dependencies for Karrierekrake desktop + search/apply.
# Heavy optional UIs (Streamlit) are not required for the PySide6 app.

rapidfuzz>=3.6.0
httpx>=0.27.0
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.5
playwright>=1.40
pandas>=2.1
pypdf>=6.16.1
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.0
python-jobspy>=1.1.0
PySide6>=6.6
# Optional: diagnostic/tests only (NOT production geo). Production = Google Maps.
pgeocode>=0.4.0
# geopy unused in production path (Nominatim removed).
# Floor pins for transitive deps with known High/Critical advisories when
# resolved from loose lower bounds (OSV / pip-audit defense in depth).
anyio>=4.14.2
idna>=3.15
protobuf>=5.29.6
# Optional: Gmail / Calendar OAuth (features degrade gracefully if missing)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth>=2.23.0
keyring>=24.0.0
tenacity>=8.2.0
# Calendar scheduling engine (PR31)
dateparser>=1.2.0
icalendar>=5.0.0
holidays>=0.40
# Optional: Günther embedded local LLM (EXE may ship without it; heuristic/fail-closed otherwise)
# llama-cpp-python>=0.3.0
# Optional: richer JSON-LD / HTML extraction for contact discovery (PR25); bs4/lxml fallback always works
# extruct>=0.16.0
# selectolax>=0.3.0
# trafilatura>=1.6.0
# Follow-up reminders ticker (PR32); stable pin only — no pre-releases
APScheduler>=3.10.4,<4.0
cryptography>=42.0.0
